#include<stdio.h>
#include<conio.h>
int main()
{
printf("enter a number\n");
scanf("%d",&num);
printf("factors of %d are:\n",num);
while(count<=num)
{
if(num%count==0)
{
printf("%d\n",count);
}
count++;
}
}
